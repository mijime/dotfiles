
function trash_clean(){
  time \rm -rf ~/.Trash/*
}

function rm_expand(){
  [[ -z $* ]] && return 1
  trash_dir=~/.Trash/$(date +%Y/%m/%d/%H%M%S)
  mkdir -p $trash_dir
  for target in $*
  do
    mv "$target" $trash_dir/
  done
}

alias rm=rm_expand

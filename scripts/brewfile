#!/usr/bin/env ruby
# frozen_string_literal: true

if platform? :darwin
  use 'bash', 'gawk', 'coreutils', 'findutils'

  use 'reattach-to-user-namespace'
  use 'datawire/blackbird/telepresence'

  use 'rust'
  use 'python', 'pipenv'
end

use 'bat'
use 'cmake'
use 'direnv'
use 'fzf'
use 'ghq'
use 'graphviz'
use 'jq'
use 'nkf'
use 'pwgen'
use 'starship'
use 'tmux'
use 'tree'
use 'vim'
use 'git', 'gibo', 'tig', 'hub'
use 'awscli', 'azure-cli'
use 'circleci'
use 'terraformer', 'tfenv'

use 'go', 'goenv'
use 'node'
use 'protobuf'
use 'shellcheck'
use 'vegeta'
use 'golangci/tap/golangci-lint'

use 'kind'
use 'kubernetes-cli'
use 'kustomize'
use 'helm'
use 'skaffold'
use 'stern'

use 'w3m' if ENV['BREW_MACHINE_NAME'] == 'home'
